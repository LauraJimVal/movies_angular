export interface Pelicula { //crear un tipo personalizado llamado Pelicula
  // el orden es indiferente, pero tienen que estar todos informados
  id: number,
  titulo: string,
  sinopsis: string,
  img: string,
  anio: number,
  direccion: string
}
